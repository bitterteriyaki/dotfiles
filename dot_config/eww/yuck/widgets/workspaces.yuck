(defwidget workspaces []
  (box
    :class "workspaces"
    :spacing 4
    (for entry in "[1, 2, 3, 4, 5, 6]"
      (eventbox
        :onclick "bspc desktop -f ${entry}"
        (label :text {focused_desktop == entry ? '' : ''})
      )
    )
  )
)

