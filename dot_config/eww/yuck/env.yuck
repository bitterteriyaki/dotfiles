(defpoll time :interval "1s" "scripts/time.sh")

(deflisten window_title :initial "None" `scripts/window_title.sh`)
(deflisten focused_desktop :initial "1" `scripts/workspaces.sh`)

(defvar current_volume 100)
(defvar reveal_volume_controller false)

