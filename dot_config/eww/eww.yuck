(include "yuck/layout.yuck")
(include "yuck/env.yuck")

;;
;; Main window
;;

(defwindow main
  :monitor 1
  :reserve (struts :side "top" :distance "45px")
  :stacking "bg"
  :exclusive true
  :namespace "eww"
  :geometry (geometry
    :x "0%"
    :y "8px"
    :width "1895px"
    :height "40px"
    :anchor "top center"
  )
  (bar)
)
